{
  "name": "parcel-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "rm -rf ./dev-build && rm -rf ./.cache && parcel src/pages/*.html --out-dir dev-build",
    "build": "rm -rf ./dist && rm -rf ./.cache && parcel build src/pages/*.html",
    "clean": "rm -rf ./dev-build && rm -rf ./dist && rm -rf ./.cache"
  },
  "keywords": [],
  "author": "@toniG-78",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "postcss-preset-env": "^6.7.0",
    "sass": "^1.41.0"
  },
  "dependencies": {}
}
